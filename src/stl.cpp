// Standard Template Library (STL)
// A few good once to start with...
#include <vector>
#include <list>
#include <map>
#include <unordered_map>
#include <string>

std::vector<int> Array;
std::list<int> LinkedList;
std::map<std::string, int> Map;
std::unordered_map<std::string, int> UnorderedMap;

