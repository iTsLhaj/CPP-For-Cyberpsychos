// Conditional Statements
void Execute(int Count)
{
    // Will a certain block of statements be executed here ?
    if(Count > 0)
    {
        // do something
        // i.e. Statement1;
        // i.e. Statement2;
    } else if (Count == 0) // Executes if the statement isn't executed in either "if" or "else"
    {
        // do something
        // i.e. Statement1;
        // i.e. Statement2;
    } else // executes if our condition is false
    {
        // do something
        // i.e. Function();
    }
}
