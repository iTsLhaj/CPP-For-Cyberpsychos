// While Loop
void Init()
{
    // 0. Initialization Expression
    int i = 0;

    // 1. Loops until it is terminated by a condition
    while (i < 10)
    {
        // i.e, StatementX;

        // Update Expression
        i++;
    }

    // 1. u can use do-while loop the same way
    do
    {
        // i.e, StatementX;

        // Update Expression
        i++;
    }while(x < 10)
}
