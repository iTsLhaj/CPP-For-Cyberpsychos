
/*
a special function in all C++ programs;
it is the function called when the program is run. 
The execution of all C++ programs begins with the main function,
regardless of where the function is actually located within the code.
*/
int main()
{
    return 0; // A C++ APPLICATION MUST RETURN A SMALL NUMBER, TYPICALLY 0, TO EXIT THE APPLICATIOn
              // return 0 means that the program executed successfully 
}